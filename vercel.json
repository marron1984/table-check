{
  "installCommand": "cd crm && npm install",
  "buildCommand": "cd crm && npx prisma generate && npm run build",
  "outputDirectory": "crm/.next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/jobs/process-sync",
      "schedule": "*/5 * * * *"
    }
  ]
}
